import userService from '../services/userService.js';

const userController = {
    create(req, res, next) {
        try {
            
        } catch (err) {}
    }
};

export default userController;
